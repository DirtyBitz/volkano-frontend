/*
import { shallow, ShallowWrapper } from 'enzyme'
import Navigation from '../Navigation'

const props = {
  userData: undefined,
}
*/

describe('Layout component', () => {
  //let navigation: ShallowWrapper<any>

  beforeEach(() => {
    //layout = shallow(<Navigation {...props} />)
  })

  it('Has a link to the homepage')

  it('Has a link to sign in page when no userData is set')

  it('Contains link to profile page when signed in')

  it('Should show name of the user when signed in')
})
